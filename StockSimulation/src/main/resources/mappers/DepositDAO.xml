<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.ixon.stock.dao.DepositDAO">

    <insert id="insertDeposit" parameterType="com.ixon.stock.dto.DepositDTO">
/* DepositDAO.xml | insertDeposit */
        INSERT INTO deposit
               (model, day, stockcode, tramount)
        VALUES (#{model}, #{day}, #{stockcode}, #{tramount})
    </insert>
</mapper>